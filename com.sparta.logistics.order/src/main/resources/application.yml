spring:
  application:
    name: order
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:~/order;AUTO_SERVER=true'    # H2 DB ?? ?? (Embedded Mode)
    username: sa              # H2 DB ?? ID (??? ??)
    password:                 # H2 DB ?? PW (??? ??)
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true      # ?? ?? ?? (??)
        show_sql: true        # ?? ?? ??

#server:
#  port: 19092